server:
  port: 8081

spring:
  application:
    name: ConfigServer
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Swoodwal/config-files/
          skipSslValidation: true
